extends layout

block content

  h1= title
  - let result = results
  div(class= "container-fluid")
    div(class = "col m-1")
      each item,index in result
        - let destination = `dispatch(\"/dolphin/update?id=${item._id}\")`
        div(class="row-sm-2" class="m-1") Item #{item.name}(#{item.age}) is #{item.weight}
        button.btn( class="btn-primary m-1", onclick= destination) Update
  script.
    function dispatch(path){
      window.location.href=path
    }
